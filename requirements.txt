# Core hood UI (main.py)
eel==0.16.0
pyttsx3==2.90
SpeechRecognition==3.10.4
playsound==1.2.2
pywin32==306; sys_platform == "win32"
pypiwin32==223; sys_platform == "win32"

# Microphone input for SpeechRecognition. PyAudio needs the PortAudio
# system library on Linux/macOS (e.g. `apt install portaudio19-dev` or
# `brew install portaudio`) before `pip install pyaudio` will build.
PyAudio==0.2.14

# Floating cursor companion / digital tutor (jarvis_overlay.py)
PyQt5==5.15.11
requests==2.32.3
